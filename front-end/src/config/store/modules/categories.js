const state = {
  all: ['Dúvida', 'Conta violada', 'Reembolso']
}

const getters = {
  allCategories: (state) => state.all
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  actions,
  mutations
}
