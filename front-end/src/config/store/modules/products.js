const state = {
  all: ['Outros', 'General War', 'Barbarians']
}

const getters = {
  allProducts: (state) => state.all
}

const mutations = {}

const actions = {}

export default {
  state,
  getters,
  actions,
  mutations
}
